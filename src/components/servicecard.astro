---
const { title, description, icon } = Astro.props;
---
<div class="bg-gray-800 p-8 rounded-xl shadow-xl border border-gray-700 hover:border-blue-500 transition-all duration-300 transform hover:scale-105 text-center">
  <div class="w-16 h-16 mx-auto mb-6 flex items-center justify-center rounded-full bg-gray-700 border-2 border-transparent hover:border-blue-500 transition-all duration-300">
    <span class={`text-3xl text-blue-500 ${icon}`}></span>
  </div>
  <h3 class="text-2xl font-bold text-white mb-2">{title}</h3>
  <p class="text-gray-400">{description}</p>
</div>